import React from 'react';

const Footer = (props) => {
    return (
        <footer className="wrapper__footer">
            <div className="container">Footer Component</div>
        </footer>
    )
}

export default Footer;
